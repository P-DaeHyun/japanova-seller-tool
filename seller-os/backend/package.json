{
  "name": "japanova-seller-os-backend",
  "version": "2.1.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "bootstrap-sync": "node src/bootstrapSync.js",
    "start": "node src/server.js",
    "start:render": "npm run migrate && npm run bootstrap-sync && node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "multer": "^2.0.2",
    "pg": "^8.13.1"
  }
}
